import type { ExportPayload, ProjectConfig } from './types';

const escapeHtml = (value: unknown) =>
  String(value ?? '')
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;');

export const dashboardCss = `
:root{--bg:#eef2f7;--surface:#fff;--surface-soft:#f7f9fc;--line:#d8e0ea;--text:#11213b;--muted:#6e8098;--shadow-lg:0 16px 38px rgba(15,23,42,.08);--shadow-sm:0 8px 20px rgba(15,23,42,.05)}
*{box-sizing:border-box}body{margin:0;font-family:Inter,Segoe UI,Arial,sans-serif;color:var(--text);background:var(--bg)}button,input,select{font:inherit}
.kpi-export-wrap{max-width:1600px;min-width:1260px;margin:0 auto;padding:24px 18px 28px}
.preview-canvas{border:1px solid var(--line);border-radius:30px;box-shadow:var(--shadow-lg);margin:0 auto;background:#fff;min-height:860px;padding:22px;max-width:1600px}
.slot-stack{display:grid;gap:18px}.preview-grid-two{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:18px;align-items:stretch}.preview-grid-two>.slot-stack{height:100%}.preview-grid-three{display:grid;grid-template-columns:1fr 1fr 1fr;gap:18px;margin-top:18px}
.surface-card{background:var(--surface);border:1px solid var(--line);border-radius:22px;box-shadow:var(--shadow-sm);padding:22px;min-height:112px;overflow:hidden}.surface-card.table-card{height:100%;display:flex;flex-direction:column}.header-block{background:var(--surface);border:1px solid var(--line);border-radius:26px;box-shadow:var(--shadow-sm);padding:26px}
.card-title{margin:0;color:var(--text);font-size:22px;line-height:1.15}.card-subtitle{margin:8px 0 0;color:var(--muted);font-size:14px;line-height:1.45}.field-label{display:block;margin:0 0 8px;color:#31435e;font-size:12px;font-weight:800;letter-spacing:.04em;text-transform:uppercase}
.filter-header-premium{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.filter-grid-premium{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:14px;margin-top:18px}.ui-select{width:100%;border:1px solid var(--line);border-radius:14px;background:#fff;color:var(--text);padding:12px 14px}.reset-button{border:0;border-radius:14px;background:#11213b;color:#fff;padding:11px 16px;cursor:pointer}
.kpi-value{margin-top:18px;font-size:42px;font-weight:900;line-height:1.05;color:#2464bf}.progress-total-line{display:flex;align-items:baseline;gap:10px;margin-top:14px;color:var(--muted)}.progress-total-line strong{font-size:34px;color:#11213b}
.progress-bars{display:grid;grid-template-columns:repeat(3,1fr);overflow:hidden;border-radius:18px;margin-top:18px}.progress-segment{min-height:86px;color:#fff;padding:16px}.progress-segment small{display:block;font-weight:800;opacity:.95}.progress-segment span{display:block;margin-top:6px;font-size:22px;font-weight:850}
.progress-list{display:grid;gap:14px;margin-top:18px}.progress-row{display:grid;grid-template-columns:16px 1.2fr 1fr auto;align-items:center;gap:12px;color:#334155}.status-square{width:14px;height:14px;border-radius:4px;display:inline-block}.progress-value,.progress-percent{font-weight:800}
.chart-fixed-height{height:320px;margin-top:14px}.chart-fixed-height.chart-wide{height:360px}.svg-chart{width:100%;height:100%;display:block;overflow:visible}.chart-label{font-size:12px;fill:#60748f}.chart-axis{stroke:#d8e0ea;stroke-width:1}.chart-grid{stroke:#edf1f6;stroke-width:1}.chart-line{fill:none;stroke-width:3;stroke-linecap:round;stroke-linejoin:round}.chart-point{stroke:#fff;stroke-width:2}.legend-text{font-size:12px;fill:#666}.legend-swatch{rx:0;ry:0}.svg-tip{pointer-events:none;opacity:0;transition:opacity .12s ease}.svg-hover:hover .svg-tip{opacity:1}.tip-box{fill:#fff;stroke:#d8e0ea;stroke-width:1;filter:drop-shadow(0 8px 18px rgba(15,23,42,.12))}.tip-title{font-size:16px;fill:#11213b;font-weight:800}.tip-text{font-size:16px;fill:#11213b;font-weight:500}.tip-swatch{rx:0;ry:0}.tip-caret{fill:#fff;stroke:#d8e0ea;stroke-width:1}
.donut-grid{display:grid;grid-template-columns:300px 1fr;gap:24px;align-items:center}.donut-chart-box{height:280px;position:relative}.donut-legend-premium{display:grid;gap:18px}.donut-legend-item{display:grid;grid-template-columns:18px 1fr;gap:14px;align-items:start}.donut-legend-name{font-size:18px;font-weight:800}.donut-legend-value{font-size:17px;font-weight:850;margin-top:3px}.donut-legend-sub{color:var(--muted);font-size:14px;margin-top:2px}
.summary-table-wrap{flex:1;overflow:visible;margin-top:14px;border:1px solid #e6edf4;border-radius:18px}.summary-table{width:100%;border-collapse:collapse;font-size:14px;min-width:520px}.summary-table th,.summary-table td{padding:12px 10px;border-top:1px solid #edf1f6;text-align:left}.summary-table th{color:#60748f;background:#f7f9fc;font-size:12px;text-transform:uppercase;letter-spacing:.03em;border-top:none}.summary-table th:nth-child(n+2),.summary-table td:nth-child(n+2){text-align:right;font-variant-numeric:tabular-nums}
.note-body{margin-top:14px;line-height:1.65;color:#334155;white-space:pre-wrap}
@media(max-width:1100px){.kpi-export-wrap{min-width:0;padding:12px}.preview-canvas{border-radius:18px;padding:14px}.preview-grid-two,.preview-grid-three,.filter-grid-premium,.donut-grid{grid-template-columns:1fr}.progress-bars{grid-template-columns:1fr}}
`;

export const runtimeScript = `
const FILTER_ALL='__all__';
const fmt=v=>new Intl.NumberFormat('ro-RO',{maximumFractionDigits:2}).format(Number(v||0));
const fmtPercent=v=>Number(v||0).toFixed(1).replace('.',',')+'%';
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
let currentYear=payload.selectedYear;
let filterValues={...(payload.filterValues||{})};
function years(){return [...new Set(payload.dataset.rows.map(r=>Number(r.an)).filter(Number.isFinite))].sort((a,b)=>a-b)}
function splitMulti(v){return String(v??'').split(',').map(i=>i.trim()).filter(Boolean)}
function matches(r,f,v){if(v===FILTER_ALL)return true;if(f==='judet')return splitMulti(r[f]).includes(String(v));return String(r[f]??'')===String(v)}
function unique(field){const values=field==='judet'?payload.dataset.rows.flatMap(r=>splitMulti(r[field])):payload.dataset.rows.map(r=>r[field]).filter(v=>v!==null&&v!==undefined&&v!=='');return [...new Set(values)].sort((a,b)=>String(a).localeCompare(String(b),'ro'))}
function fieldLabel(f){return({an:'AN',cod_ruta:'COD RUTĂ',nume_ruta:'NUME RUTĂ',status:'STATUS',judet:'JUDEȚE'}[f]||String(f).toUpperCase())}
function filteredRows(){return payload.dataset.rows.filter(r=>Number(r.an)===Number(currentYear)).filter(r=>Object.entries(filterValues).every(([f,v])=>matches(r,f,v)))}
function sum(rows,field){return rows.reduce((a,r)=>a+Number(r[field]||0),0)}
function compute(rows){const values={};payload.metrics.forEach(m=>{if(m.aggregation==='sum')values[m.id]=sum(rows,m.field);if(m.aggregation==='count')values[m.id]=rows.length;if(m.aggregation==='distinctCount')values[m.id]=new Set(rows.map(r=>r[m.distinctField||m.field])).size;if(m.aggregation==='average')values[m.id]=rows.length?sum(rows,m.field)/rows.length:0;if(m.aggregation==='conditionalSum')values[m.id]=sum(rows.filter(r=>String(r[m.conditionField]??'')===String(m.conditionValue??'')),m.field);if(m.aggregation==='percentOfTotal'){const n=values[m.numeratorMetric]||0,d=values[m.denominatorMetric]||0;values[m.id]=d?n/d*100:0}if(m.aggregation==='formula'){const safe=String(m.expression||'0').replace(/\\b[a-zA-Z_][a-zA-Z0-9_]*\\b/g,t=>String(values[t]||0));values[m.id]=/^[\\d+\\-*/().\\s]+$/.test(safe)?Number(Function('"use strict";return('+safe+')')())||0:0}if(!Number.isFinite(values[m.id]))values[m.id]=0});return values}
function metricOf(id){return (payload.metrics||[]).find(m=>m.id===id)||{}}
function colorOf(field,index){return metricOf(field).color||payload.statusColors?.[field]||['#2464bf','#df8b22','#1f8f54','#7c3aed','#0ea5e9','#ef4444'][index%6]}
function labelOf(field){return metricOf(field).label||payload.statusLabels?.[field]||field}
function st(c,k,f){return c&&c.style&&c.style[k]!==undefined?c.style[k]:f}
function showTip(c){return st(c,'showTooltip',true)!==false}
function summary(rows){const values=compute(rows),keys=['in_utilizare','in_constructie','in_planificare'],total=keys.reduce((a,k)=>a+Number(values[k]||0),0);return{values,statusSummary:keys.map((k,i)=>({key:k,name:payload.statusLabels[k]||labelOf(k),value:Number(values[k]||0),percent:total?Number(values[k]||0)/total*100:0,color:colorOf(k,i)}))}}
function trend(){return years().map(y=>{const rows=payload.dataset.rows.filter(r=>Number(r.an)===Number(y)).filter(r=>Object.entries(filterValues).every(([f,v])=>f==='an'||matches(r,f,v)));return{an:y,...compute(rows)}})}
function routeSummary(rows){const groups=new Map();rows.forEach(r=>{const k=String(r.ruta||r.nume_ruta||r.cod_ruta||'Nespecificat');groups.set(k,[...(groups.get(k)||[]),r])});return[...groups.entries()].map(([ruta,rs])=>({ruta,values:compute(rs)})).sort((a,b)=>Number(b.values.total_km||0)-Number(a.values.total_km||0))}
function titleBlock(c){return '<h2 class="card-title" style="font-size:'+st(c,'titleSize',22)+'px;color:'+st(c,'titleColor','#11213b')+'">'+esc(c.title||'')+'</h2>'+(c.subtitle?'<p class="card-subtitle" style="font-size:'+st(c,'subtitleSize',14)+'px;color:'+st(c,'subtitleColor','#6e8098')+'">'+esc(c.subtitle)+'</p>':'')}
function niceTicks(max){const safe=Math.max(1,max),step=safe/4;return[0,step,step*2,step*3,safe]}
function yAxis(ticks,w,h,p,max){let out='';ticks.forEach(t=>{const y=h-p-(t/max)*(h-p*2);out+='<line class="chart-grid" x1="'+p+'" y1="'+y+'" x2="'+(w-p)+'" y2="'+y+'"/><text class="chart-label" x="'+(p-8)+'" y="'+(y+4)+'" text-anchor="end">'+fmt(t)+'</text>'});out+='<line class="chart-axis" x1="'+p+'" y1="'+(h-p)+'" x2="'+(w-p)+'" y2="'+(h-p)+'"/><line class="chart-axis" x1="'+p+'" y1="'+p+'" x2="'+p+'" y2="'+(h-p)+'"/>';return out}
function chartLegend(fields,w){const itemW=140,total=fields.length*itemW,start=(w-total)/2;return'<g>'+fields.map((f,i)=>'<rect class="legend-swatch" x="'+(start+i*itemW)+'" y="12" width="42" height="12" fill="'+colorOf(f,i)+'"/><text class="legend-text" x="'+(start+i*itemW+50)+'" y="23">'+esc(labelOf(f))+'</text>').join('')+'</g>'}
function groupTooltip(x,y,title,fields,item,enabled,w){if(!enabled)return'';const boxW=246,rowH=25,titleGap=18,boxH=48+fields.length*rowH,boxX=Math.max(8,Math.min(w-boxW-8,x-boxW/2)),boxY=Math.max(42,y-boxH-18),caretX=Math.max(boxX+10,Math.min(boxX+boxW-10,x));let rows=fields.map((f,i)=>'<rect class="tip-swatch" x="'+(boxX+18)+'" y="'+(boxY+42+titleGap+i*rowH-13)+'" width="16" height="16" fill="'+colorOf(f,i)+'"/><text class="tip-text" x="'+(boxX+42)+'" y="'+(boxY+42+titleGap+i*rowH)+'">'+esc(labelOf(f))+': '+fmt(Number(item[f]||0))+'</text>').join('');return'<g class="svg-tip"><rect class="tip-box" x="'+boxX+'" y="'+boxY+'" width="'+boxW+'" height="'+boxH+'" rx="6"/><path class="tip-caret" d="M'+(caretX-6)+' '+(boxY+boxH)+' L'+caretX+' '+(boxY+boxH+8)+' L'+(caretX+6)+' '+(boxY+boxH)+' Z"/><text class="tip-title" x="'+(boxX+18)+'" y="'+(boxY+25)+'">'+esc(title)+'</text>'+rows+'</g>'}
function singleTooltip(x,y,label,value,enabled,w){if(!enabled)return'';const field='__single__',boxW=220,boxH=58,boxX=Math.max(8,Math.min(w-boxW-8,x-boxW/2)),boxY=Math.max(8,y-boxH-18),caretX=Math.max(boxX+10,Math.min(boxX+boxW-10,x));return'<g class="svg-tip"><rect class="tip-box" x="'+boxX+'" y="'+boxY+'" width="'+boxW+'" height="'+boxH+'" rx="6"/><path class="tip-caret" d="M'+(caretX-6)+' '+(boxY+boxH)+' L'+caretX+' '+(boxY+boxH+8)+' L'+(caretX+6)+' '+(boxY+boxH)+' Z"/><text class="tip-title" x="'+(boxX+18)+'" y="'+(boxY+24)+'">'+esc(label)+'</text><text class="tip-text" x="'+(boxX+18)+'" y="'+(boxY+47)+'">'+fmt(value)+'</text></g>'}
function lineChart(data,fields,c){const w=700,h=300,p=52,top=58,max=Math.max(1,...data.flatMap(d=>fields.map(f=>Number(d[f]||0))));const ticks=niceTicks(max),xs=data.map((_,i)=>p+i*((w-p*1.35-p)/Math.max(1,data.length-1)));let out='<svg class="svg-chart" viewBox="0 0 '+w+' '+h+'" role="img">'+chartLegend(fields,w)+yAxis(ticks,w,h,p,max);let hover='';data.forEach((d,i)=>{if(i%2===0)out+='<text class="chart-label" x="'+xs[i]+'" y="'+(h-8)+'" text-anchor="middle">'+d.an+'</text>'});fields.forEach((f,idx)=>{const color=colorOf(f,idx),points=data.map((d,i)=>({x:xs[i],y:h-p-(Number(d[f]||0)/max)*(h-p*2),v:Number(d[f]||0)}));out+='<polyline class="chart-line" points="'+points.map(pt=>pt.x+','+pt.y).join(' ')+'" stroke="'+color+'"/>';points.forEach(pt=>{out+='<circle class="chart-point" cx="'+pt.x+'" cy="'+pt.y+'" r="4" fill="'+color+'"/>'})});data.forEach((d,i)=>{const left=i===0?p:xs[i]-(xs[i]-xs[i-1])/2,right=i===data.length-1?w-p:xs[i]+(xs[i+1]-xs[i])/2;hover+='<g class="svg-hover"><rect x="'+left+'" y="'+top+'" width="'+(right-left)+'" height="'+(h-p-top)+'" fill="transparent"/>'+groupTooltip(xs[i],h*.72,String(d.an),fields,d,showTip(c),w)+'</g>'});return out+hover+'</svg>'}
function barChart(data,fields,c,stacked=false){const wide=c&&c.slot==='bottom',w=wide?1180:700,h=wide?340:300,p=wide?62:52,top=58,max=Math.max(1,...data.map(d=>stacked?fields.reduce((a,f)=>a+Number(d[f]||0),0):Math.max(...fields.map(f=>Number(d[f]||0)))));const ticks=niceTicks(max),plotW=w-p*1.2-p,band=plotW/Math.max(1,data.length);let out='<svg class="svg-chart" preserveAspectRatio="none" viewBox="0 0 '+w+' '+h+'">'+chartLegend(fields,w)+yAxis(ticks,w,h,p,max),hover='';data.forEach((d,i)=>{const x=p+i*band+band*.12;out+='<text class="chart-label" x="'+(x+band*.38)+'" y="'+(h-8)+'" text-anchor="middle">'+d.an+'</text>';let base=h-p;fields.forEach((f,idx)=>{const val=Number(d[f]||0),bh=val/max*(h-p*2),bw=stacked?band*.68:(band*.68/fields.length),bx=stacked?x:x+idx*bw,by=stacked?base-bh:h-p-bh,color=colorOf(f,idx);if(stacked)base-=bh;out+='<rect x="'+bx+'" y="'+by+'" width="'+Math.max(4,bw-3)+'" height="'+Math.max(0,bh)+'" rx="8" fill="'+color+'"/>'});hover+='<g class="svg-hover"><rect x="'+(p+i*band)+'" y="'+top+'" width="'+band+'" height="'+(h-p-top)+'" fill="transparent"/>'+groupTooltip(x+band*.34,h*.72,String(d.an),fields,d,showTip(c),w)+'</g>'});return out+hover+'</svg>'}
function donut(items,c){let total=items.reduce((a,i)=>a+i.value,0)||1,angle=-90,parts='',tips='';items.forEach(i=>{const slice=i.value/total*360;const a1=angle*Math.PI/180,a2=(angle+slice)*Math.PI/180,large=slice>180?1:0,x1=140+108*Math.cos(a1),y1=140+108*Math.sin(a1),x2=140+108*Math.cos(a2),y2=140+108*Math.sin(a2),mid=(angle+slice/2)*Math.PI/180,tx=140+78*Math.cos(mid),ty=140+78*Math.sin(mid);parts+='<path d="M140 140 L'+x1+' '+y1+' A108 108 0 '+large+' 1 '+x2+' '+y2+' Z" fill="'+i.color+'"/>';tips+='<g class="svg-hover"><path d="M140 140 L'+x1+' '+y1+' A108 108 0 '+large+' 1 '+x2+' '+y2+' Z" fill="transparent"/>'+singleTooltip(tx,ty,i.name,i.value,showTip(c),280)+'</g>';angle+=slice});return'<svg class="svg-chart" viewBox="0 0 280 280">'+parts+'<circle cx="140" cy="140" r="66" fill="#fff"/><text x="140" y="134" text-anchor="middle" class="chart-label">Total</text><text x="140" y="160" text-anchor="middle" font-size="22" font-weight="850" fill="#11213b">'+fmt(total)+'</text>'+tips+'</svg>'}
function renderComponent(c,rows,s){const fields=c.fields&&c.fields.length?c.fields:['in_utilizare','in_constructie','in_planificare'];if(c.type==='kpi'){const m=metricOf(c.metric);return'<div class="surface-card">'+titleBlock(c)+'<div class="kpi-value" style="font-size:'+st(c,'valueSize',42)+'px;color:'+st(c,'valueColor',m.color||'#2464bf')+'">'+fmt(s.values[c.metric]||0)+(m.suffix||'')+'</div></div>'}if(c.type==='progress_status'){return'<div class="surface-card">'+titleBlock(c)+'<div class="progress-total-line"><strong style="font-size:'+st(c,'valueSize',34)+'px;color:'+st(c,'valueColor','#11213b')+'">'+fmt(s.values.total_km||0)+' km</strong><span>total analizat</span></div><div class="progress-bars">'+s.statusSummary.map(i=>'<div class="progress-segment" style="background:'+i.color+'"><small>'+esc(i.name)+'</small><span>'+fmt(i.value)+' km</span></div>').join('')+'</div><div class="progress-list">'+s.statusSummary.map(i=>'<div class="progress-row"><span class="status-square" style="background:'+i.color+'"></span><div>'+esc(i.name)+'</div><div class="progress-value">'+fmt(i.value)+' km</div><div class="progress-percent">'+fmtPercent(i.percent)+'</div></div>').join('')+'</div></div>'}if(c.type==='donut_status'){return'<div class="surface-card">'+titleBlock(c)+'<div class="donut-grid"><div class="donut-chart-box">'+donut(s.statusSummary,c)+'</div><div class="donut-legend-premium">'+s.statusSummary.map(i=>'<div class="donut-legend-item"><span class="status-square" style="background:'+i.color+'"></span><div><div class="donut-legend-name">'+esc(i.name)+'</div><div class="donut-legend-value">'+fmt(i.value)+' km</div><div class="donut-legend-sub">'+fmtPercent(i.percent)+' din total</div></div></div>').join('')+'</div></div></div>'}if(c.type==='line_trend')return'<div class="surface-card">'+titleBlock(c)+'<div class="chart-fixed-height">'+lineChart(trend(),fields,c)+'</div></div>';if(c.type==='bar_compare')return'<div class="surface-card">'+titleBlock(c)+'<div class="chart-fixed-height '+(c.slot==='bottom'?'chart-wide':'')+'">'+barChart(trend(),fields,c,false)+'</div></div>';if(c.type==='stacked_status')return'<div class="surface-card">'+titleBlock(c)+'<div class="chart-fixed-height '+(c.slot==='bottom'?'chart-wide':'')+'">'+barChart(trend(),fields,c,true)+'</div></div>';if(c.type==='summary_table'){const top=routeSummary(rows).slice(0,c.limit||10);return'<div class="surface-card table-card">'+titleBlock(c)+'<div class="summary-table-wrap"><table class="summary-table"><thead><tr><th>Rută</th><th>Km</th><th>Segmente</th><th>Loturi</th></tr></thead><tbody>'+top.map(i=>'<tr><td>'+esc(i.ruta)+'</td><td>'+fmt(i.values.total_km||0)+' km</td><td>'+fmt(i.values.segmente||0)+'</td><td>'+fmt(i.values.loturi||0)+'</td></tr>').join('')+'</tbody></table></div></div>'}if(c.type==='note_block')return'<div class="surface-card">'+titleBlock(c)+'<div class="note-body" style="font-size:'+st(c,'valueSize',16)+'px;color:'+st(c,'valueColor','#334155')+'">'+esc(c.body||'')+'</div></div>';return''}
function render(){const rows=filteredRows(),s=summary(rows),groups={filter:[],upper_left:[],upper_center:[],upper_right:[],main_left:[],main_right:[],bottom_left:[],bottom_right:[],bottom:[],lower_left:[],lower_center:[],lower_right:[]};payload.components.forEach(c=>{if(groups[c.slot])groups[c.slot].push(c)});const filterHtml=groups.filter.map(c=>'<div class="surface-card"><div class="filter-header-premium"><div>'+titleBlock(c)+'</div><button class="reset-button" id="resetFiltersBtn" type="button">Reset filtre</button></div><div class="filter-grid-premium">'+(c.fields||[]).map(f=>'<div><label class="field-label">'+esc(fieldLabel(f))+'</label><select class="ui-select filter-select" data-field="'+esc(f)+'"><option value="'+FILTER_ALL+'">Toate valorile</option>'+unique(f).map(v=>'<option value="'+esc(v)+'" '+(String(filterValues[f])===String(v)?'selected':'')+'>'+esc(v)+'</option>').join('')+'</select></div>').join('')+'</div></div>').join('');document.getElementById('app').innerHTML='<div class="slot-stack">'+filterHtml+'</div><div class="preview-grid-three"><div class="slot-stack">'+groups.upper_left.map(c=>renderComponent(c,rows,s)).join('')+'</div><div class="slot-stack">'+groups.upper_center.map(c=>renderComponent(c,rows,s)).join('')+'</div><div class="slot-stack">'+groups.upper_right.map(c=>renderComponent(c,rows,s)).join('')+'</div></div><div class="preview-grid-two"><div class="slot-stack">'+groups.main_left.map(c=>renderComponent(c,rows,s)).join('')+'</div><div class="slot-stack">'+groups.main_right.map(c=>renderComponent(c,rows,s)).join('')+'</div></div><div class="preview-grid-two"><div class="slot-stack">'+groups.bottom_left.map(c=>renderComponent(c,rows,s)).join('')+'</div><div class="slot-stack">'+groups.bottom_right.map(c=>renderComponent(c,rows,s)).join('')+'</div></div><div class="slot-stack" style="margin-top:18px">'+groups.bottom.map(c=>renderComponent(c,rows,s)).join('')+'</div>';bind()}
function bind(){document.querySelectorAll('.filter-select').forEach(sel=>{sel.onchange=e=>{const f=e.target.dataset.field;filterValues[f]=e.target.value;if(f==='an'&&e.target.value!==FILTER_ALL)currentYear=Number(e.target.value);render()}});const reset=document.getElementById('resetFiltersBtn');if(reset)reset.onclick=()=>{const c=payload.components.find(c=>c.type==='selective_filter');filterValues=Object.fromEntries((c?.fields||[]).map(f=>[f,FILTER_ALL]));render()}}
render();
`;

export function projectToPayload(project: ProjectConfig): ExportPayload {
  return {
    projectName: project.projectName,
    dataset: project.dataset,
    metrics: project.metrics,
    components: project.components,
    theme: project.theme,
    selectedYear: project.selectedYear,
    filterValues: project.filterValues,
    statusColors: project.statusColors,
    statusLabels: project.statusLabels,
  };
}

export function makeStandaloneHtml(project: ProjectConfig) {
  const payload = projectToPayload(project);
  return `<!doctype html>
<html lang="ro">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>${escapeHtml(project.projectName)}</title>
<style>${dashboardCss}</style>
</head>
<body style="background:${project.theme.pageBg}">
<div class="kpi-export-wrap">
  <main id="app" class="preview-canvas" style="background:${project.theme.canvasBg};min-height:${project.theme.minHeight}px;padding:${project.theme.padding}px"></main>
</div>
<script>const payload=${JSON.stringify(payload).replaceAll('</script', '<\\/script')};${runtimeScript}</script>
</body>
</html>`;
}

export function renderPreviewMarkup(project: ProjectConfig) {
  return makeStandaloneHtml(project);
}

